const ErrorHandler = () => {
  return (
    <h1 style={{ textAlign: "center" }}>
      An error occured loading this page, seems the api server is down
    </h1>
  );
};

export default ErrorHandler;
